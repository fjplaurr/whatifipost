{
  "name": "server",
  "version": "1.0.0",
  "description": "PostApp's Frontend",
  "private": true,
  "scripts": {
    "dev-build": "cd frontend/ && npx tsc && npm install && npm run build",
    "start": "node server"
  },
  "keywords": [],
  "author": "fjplaurr",
  "license": "ISC",
  "engines": {
    "node": "12.x"
  },
  "devDependencies": {
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
